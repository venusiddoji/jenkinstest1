pipeline {
     agent any
        stages {
           stage ("Build") {
               echo "Hello from Build"
               }
            stage ("Dev") {
               echo "Hello from Dev"
               }
            stage ("Test") {
               echo "Hello from Test"
               }
            stage ("Prod") {
               echo "Hello from Production"
            }
         } 
}
